Run the following command in the root of the back-end project to start the Rocket dev back-end server:
```bash
cargo run
```

- Back-end server should be running on port 8000 with URL:  http://localhost:8080